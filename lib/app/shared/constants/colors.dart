// Contain all app colors

import 'package:flutter/material.dart';

const white = Colors.white;
const black = Colors.black;

// Light color extension

const lightWarning = Color(0xFFffa726);
const lightSuccess = Color(0xFF4BB543);
const lightInfo = Color(0xFF29b6f6);

// DarkARK color extension

const darkWarning = Color(0xFFf57c00);
const darkSuccess = Color(0xFF388e3c);
const darkInfo = Color(0xFF0288d1);
